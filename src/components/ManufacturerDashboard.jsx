import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const ManufacturerDashboard = () => {
  return (
    <div clas sName="min-h-screen bg-background p-6">
      <Card>
        <CardHeader>
          <CardTitle>Manufacturer Dashboard</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Manufacturer Dashboard functionality will be restored after full conversion.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default ManufacturerDashboard;